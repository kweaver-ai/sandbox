# Python Basic Template
# Minimal Python 3.11 environment for general-purpose code execution
# Based on sandbox-executor-base which contains the executor daemon

ARG BASE_IMAGE=sandbox-executor-base:latest
FROM ${BASE_IMAGE}

# Metadata
LABEL template.name="python-basic"
LABEL template.description="Minimal Python 3.11 environment"
LABEL template.version="1.0.0"

# No additional installations needed
# The base image already includes Python 3.11 and all executor components
# CMD is inherited from the base image (executor daemon)
